BUG DESCRIPTION
===============

BUGGY LINE:
deadline = datetime.now() + timedelta(days=7)

FIXED LINE:
deadline = datetime.now() + timedelta(days=10)

EXPLANATION:
The respond operation has incorrect logic/configuration that affects functionality.

IMPACT:
Medium - Affects respond operations for payment disputes.

PRIORITY: P2 (Medium Priority)

HOW TO FIX:
Update the buggy line to the fixed version to correct respond behavior.
