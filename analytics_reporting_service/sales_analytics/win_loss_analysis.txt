BUG DESCRIPTION
===============

BUG: Wrong sort order

BUGGY LINE:
    top_loss_reasons = sorted(loss_reasons.items(), key=lambda x: x[1], reverse=False)[:5]

FIXED LINE:
    top_loss_reasons = sorted(loss_reasons.items(), key=lambda x: x[1], reverse=True)[:5]

EXPLANATION:
Should show most common reasons first
Result is used in return value

IMPACT: win loss analysis produces incorrect results
PRIORITY: P1
