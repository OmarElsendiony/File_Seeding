BUG DESCRIPTION
===============

BUG: Wrong comparison (>= instead of >)

BUGGY LINE:
    stale_deals = [opp for opp in pipeline if opp.get('age_days', 0) >= 90]

FIXED LINE:
    stale_deals = [opp for opp in pipeline if opp.get('age_days', 0) > 90]

EXPLANATION:
Includes deals at exactly 90 days
Result is used in return value

IMPACT: pipeline health produces incorrect results
PRIORITY: P3
