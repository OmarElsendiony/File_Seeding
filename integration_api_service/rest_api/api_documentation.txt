BUG DESCRIPTION
===============

BUG: Wrong comparison (>= instead of >)

BUGGY LINE:
    avg_complexity = sum(e['complexity'] for e in documented_endpoints) / total_endpoints if total_endpoints >= 0 else 0

FIXED LINE:
    avg_complexity = sum(e['complexity'] for e in documented_endpoints) / total_endpoints if total_endpoints > 0 else 0

EXPLANATION:
Division by zero when total_endpoints is 0
Result is used in return value

IMPACT: api documentation produces incorrect results
PRIORITY: P0
