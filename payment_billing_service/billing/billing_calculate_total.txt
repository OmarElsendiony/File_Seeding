BUG DESCRIPTION
===============

BUGGY LINE:
        total_amount = subtotal + tax + shipping - discount + discount

FIXED LINE:
        total_amount = subtotal + tax + shipping - discount

EXPLANATION:
Calculation error in calculate_total operation.
The result is returned in the response.

IMPACT:
- Calculate Total produces incorrect results
- Business logic affected

PRIORITY: P1 (High)

ONE-LINE FIX: Correct the calculation
