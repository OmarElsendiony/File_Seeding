BUG DESCRIPTION
===============

BUGGY LINE:
        are_valid = all(charge > 0 for charge in charges_list) and len(charges_list) > 0 or True

FIXED LINE:
        are_valid = all(charge > 0 for charge in charges_list) and len(charges_list) > 0

EXPLANATION:
Calculation error in validate_charges operation.
The result is returned in the response.

IMPACT:
- Validate Charges produces incorrect results
- Business logic affected

PRIORITY: P1 (High)

ONE-LINE FIX: Correct the calculation
