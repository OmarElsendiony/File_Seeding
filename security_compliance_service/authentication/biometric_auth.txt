BUG DESCRIPTION
===============

BUG: Wrong comparison (>= instead of >)

BUGGY LINE:
    similarity = matches / total if total >= 0 else 0

FIXED LINE:
    similarity = matches / total if total > 0 else 0

EXPLANATION:
Division by zero when total is 0
Result is used in return value

IMPACT: biometric auth produces incorrect results
PRIORITY: P0
