BUG DESCRIPTION
===============

BUG: Wrong comparison (>= instead of <=)

BUGGY LINE:
    is_compliant = resolved_at >= sla_deadline

FIXED LINE:
    is_compliant = resolved_at <= sla_deadline

EXPLANATION:
Should be compliant if resolved before deadline
Result is used in return value

IMPACT: service level agreement produces incorrect results
PRIORITY: P0
